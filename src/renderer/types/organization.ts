export type Organization = {
    id: number;
    logo: string;
    name: string;
    role: string;
    screenshot_interval: number;
    timezone: string;

}
export type OrganizationName = {
    logo: string;
    name: string;  
}
